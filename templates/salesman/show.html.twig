{% extends 'base.html.twig' %}

{% block title %}Salesman{% endblock %}

{% block body %}
    <h1>Salesman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salesman.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salesman.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ salesman.address }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ salesman.city }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ salesman.state }}</td>
            </tr>
            <tr>
                <th>Zip</th>
                <td>{{ salesman.zip }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ salesman.email }}</td>
            </tr>
            <tr>
                <th>Cell</th>
                <td>{{ salesman.cell }}</td>
            </tr>
            <tr>
                <th>License</th>
                <td>{{ salesman.license }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salesman_index') }}">back to list</a>

    <a href="{{ path('salesman_edit', {'id': salesman.id}) }}">edit</a>

    {{ include('salesman/_delete_form.html.twig') }}
{% endblock %}
